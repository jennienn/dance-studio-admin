# AGENTS.md

## 프로젝트 개요

이 프로젝트는 Next.js 14 App Router, TypeScript, Supabase 기반의 댄스학원 회원 관리 시스템이다.

주요 기능:

- 회원 등록 및 수정
- 개인레슨 4회권, 8회권, 12회권 관리
- 개인레슨 회차 기록
- 단체레슨 반 및 요일 관리
- 단체레슨 출석 등록 및 취소
- 결제 및 재등록 이력 관리
- 결제 필요 회원 조회
- 알림 발송 대상 관리

## 기술 스택

- Next.js 14 App Router
- React
- TypeScript
- Supabase Auth
- Supabase PostgreSQL
- Supabase Row Level Security
- Vercel
- npm

## 작업 원칙

1. 작업 전 관련 코드, 데이터베이스 스키마, API Route를 먼저 읽는다.
2. 기존 동작을 확인하지 않고 대규모 리팩터링하지 않는다.
3. 요청받지 않은 UI와 비즈니스 규칙은 변경하지 않는다.
4. TypeScript의 `any` 사용을 가능한 한 피한다.
5. 오류를 숨기기 위해 테스트 조건을 약화하거나 테스트를 삭제하지 않는다.
6. 구현 오류가 발견되면 테스트를 우회하지 말고 실제 코드를 수정한다.
7. 변경 후 반드시 typecheck와 관련 테스트를 실행한다.
8. 데이터베이스 변경이 필요하면 마이그레이션 파일로 작성한다.
9. 작업 완료 후 변경 파일, 테스트 결과, 남은 문제를 요약한다.

## 데이터베이스 안전 규칙

1. 운영 Supabase 데이터베이스에 테스트 데이터를 삽입하지 않는다.
2. `.env.local`의 운영 환경변수를 자동 테스트에 사용하지 않는다.
3. 테스트는 `.env.test.local` 또는 명시적으로 허용된 테스트 Supabase 프로젝트만 사용한다.
4. 테스트 실행 전에 대상 Supabase URL을 출력한다.
5. 허용된 테스트 프로젝트 URL과 다르면 테스트와 seed 실행을 즉시 중단한다.
6. `SUPABASE_SERVICE_ROLE_KEY`를 브라우저 코드나 `NEXT_PUBLIC_` 환경변수에 노출하지 않는다.
7. 실제 회원 이름, 전화번호, 결제 정보는 테스트 데이터에 사용하지 않는다.
8. 테스트 데이터는 이름 앞에 `테스트회원_` 접두사를 붙인다.
9. 테스트가 만든 행의 ID를 기록하고 해당 행만 cleanup한다.
10. 기존 데이터를 전체 삭제하거나 테이블을 truncate하지 않는다.

## 핵심 데이터 구조

대략적인 관계는 다음과 같다.

```text
members
└── enrollments
    └── enrollment_cycles
        ├── payments
        ├── sessions
        ├── cycle_schedules
        ├── attendance_logs
        └── notifications
```

정확한 컬럼과 관계는 Supabase 마이그레이션 및 실제 코드를 기준으로 판단한다.

## 반드시 검증할 비즈니스 규칙

### 개인레슨

- 4회권, 8회권, 12회권 생성
- 첫 수업 등록 전후 유효기간 계산
- 개인레슨 회차 등록, 수정, 삭제
- 잔여 회차 계산
- 잔여 1회 이하 상태
- 기간 만료 상태
- 재등록 후 이전 주기 이력 보존

### 단체레슨

- 출석 등록 및 취소
- 요일별 회원 조회
- 한 회원의 복수 요일 등록
- 반 정원 10명 제한
- 동일 회원의 복수 요일이 정원에서 중복 집계되지 않는지 검증
- 재등록 후 출석 및 결제 주기 보존

### 인증 및 권한

- 비로그인 사용자의 보호 페이지 접근 차단
- 비로그인 사용자의 API 접근 차단
- 다른 운영자의 데이터 접근 차단
- 서비스 역할 키의 클라이언트 노출 방지

### 결제

- 개인레슨 결제 이력
- 단체레슨 결제 이력
- 재등록 결제
- 결제 예정 회원 조회
- 환불 및 수강 종료 처리

## 테스트 구성

가능하면 다음 계층으로 구분한다.

### 단위 테스트

Vitest를 사용한다.

대상:

- 날짜 계산
- 만료일 계산
- 잔여 회차 계산
- 결제 예정일 계산
- 정원 계산
- 입력 검증

### 통합 테스트

테스트 전용 Supabase 프로젝트를 사용한다.

대상:

- 회원 등록
- 수강권 생성
- 결제 주기 생성
- 개인레슨 회차 저장
- 단체 출석 저장
- 재등록
- 환불
- 권한 검증

### E2E 테스트

Playwright를 사용한다.

대상:

- 로그인
- 회원 등록
- 개인레슨 등록
- 단체 출석 처리
- 재등록
- 회원 정보 수정

## 테스트 데이터

Seed 데이터에는 최소 다음 사례를 포함한다.

- 개인레슨 4회권 회원 3명
- 개인레슨 8회권 회원 3명
- 개인레슨 12회권 회원 3명
- 단체레슨 월요일 회원 5명
- 단체레슨 수요일 회원 5명
- 복수 요일 회원 3명
- 결제 예정 회원
- 기간 만료 회원
- 잔여 1회 회원
- 재등록 이력이 있는 회원

전화번호는 실제 전화번호처럼 보이지 않는 명확한 테스트 값을 사용한다.

## 명령어

작업 시작 시 `package.json`을 확인하고 실제 명령을 기준으로 실행한다.

기본 확인 명령:

```bash
npm install
npm run typecheck
npm run lint
npm run build
```

테스트 환경을 구축한 뒤 다음 명령을 제공한다.

```bash
npm run test
npm run test:integration
npm run test:e2e
npm run test:seed
npm run test:cleanup
```

## 완료 조건

작업 완료 전 다음을 확인한다.

- TypeScript 검사 통과
- 관련 단위 테스트 통과
- 관련 통합 테스트 통과
- 필요한 E2E 테스트 통과
- 운영 DB를 사용하지 않았는지 확인
- 테스트 데이터 정리 여부 확인
- 변경 사항 diff 확인
- 실패한 테스트와 미해결 문제를 숨김없이 보고
