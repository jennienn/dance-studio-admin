# 댄스학원 관리 시스템 - 개발 규칙

## 디자인 규칙 (가장 중요)

- 새 화면/컴포넌트를 만들기 전에 반드시 `src/app/globals.css`를 먼저 확인한다.
- 필요한 클래스(.panel, .badge, .info-grid 등)가 이미 있으면 그걸 쓴다.
- 없으면 인라인 스타일로 즉흥적으로 만들지 말고, globals.css에 클래스를 먼저 추가한 다음 쓴다.
  (색상은 반드시 :root에 정의된 CSS 변수만 사용 — 새 색상 임의로 추가 금지)
- 배지 색상 규칙: 개인 4회=--c4, 8회=--c8, 12회=--c12 / 단체=--group / 결제필요=--danger / 임박=--warning

## 데이터/로직 규칙

- "결제 필요 여부"와 "알림톡 발송 대상 여부" 판정은 반드시 `src/lib/business-rules.ts`의 함수만 쓴다.
  화면 컴포넌트에서 날짜 비교 로직을 새로 짜지 않는다.
- 재등록/재결제는 기존 행을 수정하지 않고 항상 새 `enrollment_cycle`을 생성한다 (이력 보존).
- 회차/출석/알림 기록은 절대 삭제하지 않는다. "수강 종료"도 삭제가 아니라 상태 변경이다.

## 작업 전 확인할 문서

- `docs/01_요구사항명세서.md` - 비즈니스 룰 전체
- `docs/02_유즈케이스.md` - 화면별 동작 시나리오
- `docs/03_API명세서.md` - API 스펙
- `docs/04_DB설계서.md` - 테이블 구조
  새 기능을 만들기 전, 관련 문서 섹션을 먼저 확인하고 시작한다.

## 아직 정책 미확정 (임의로 API 만들지 말고 먼저 질문할 것)

- 결제 환불 시 enrollment_cycle 처리 방식
- 개인레슨 "수강 정보 수정" 기능 범위
